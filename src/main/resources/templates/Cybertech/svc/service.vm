package ${basePackage}.${moduleName}.svc;

import cn.com.cybertech.commons.db.pagination.PaginationOrdersList;
import org.apache.ibatis.annotations.Param;
import org.springframework.stereotype.Repository;
import java.util.List;
import ${basePackage}.${moduleName}.po.${table.getCName()};

/**
 * ${table.tableName} - ${table.tableComment}
 *
 * @author ${author}
 * @date ${yyyy-MM-dd}
 */
public interface ${table.getCName()}Service{

    /**
     * 新增插入
     *
     * @param po 实体对象
     * @return 返回数量
     */
    long insert(${table.getCName()} po);

    /**
     * 根据主键删除对象(必须存在主键)
     *
     * @param po 条件
     * @return 数量
     */
    public long deleteById(${table.getCName()} po);

    /**
     * 根据主键更新对象(必须存在主键)
     *
     * @param po 条件
     * @return 数量
     */
    public long updateById(${table.getCName()} po);

    /**
     * 根据主键查询(必须存在主键,防止多个主键,传参)
     *
     * @param po 条件
     * @return 返回唯一对象
     */
    ${table.getCName()} getById(${table.getCName()} po);

    /**
     * 不分页查询
     *
     * @param po 查询参数
     * @return 返回集合
     */
    List<${table.getCName()}> queryAll(${table.getCName()} po);

    /**
     * 通用分页查询(模糊和精确条件组合查询，用于后期定制查询)
     *
     * @param po 查询对象
     * @return 返回对象
     */
    PaginationOrdersList<${table.getCName()}> query(${table.getCName()} po, PaginationOrdersList<${table.getCName()}> pageBean);

##    /**
##     * 精确分页查询
##     *
##     * @param po 查询对象
##     * @return 返回集合
##     */
##    List<${table.getCName()}> find(${table.getCName()} po,@Param("beginIndex") int beginIndex,@Param("pageSize") int pageSize);
##
##    /**
##     * 模糊分页查询
##     *
##     * @param po 查询对象
##     * @return 返回对象
##     */
##    List<${table.getCName()}> search(${table.getCName()} po,@Param("beginIndex") int beginIndex,@Param("pageSize") int pageSize);

    /**
     * 判断主键是否存在
     *
     * @param po 查询参数
     * @return 返回数量
     */
    boolean existById(${table.getCName()} po);
}
