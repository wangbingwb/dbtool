package ${basePackage}.${moduleName}.dao;

import org.apache.ibatis.annotations.Param;
import org.springframework.stereotype.Repository;
import java.util.List;
import ${basePackage}.${moduleName}.po.${table.getCName()}PO;
import ${basePackage}.${moduleName}.base.Token;

/**
* ${table.tableName} - ${table.tableComment}
*
* @author
* @date ${yyyy-MM-dd}
*/
@Repository
public interface ${table.getCName()}Mapper{

    /**
    * 插入
    *
    * @param po 实体对象
    * @return 返回数量
    */
    long insert(@Param("po") ${table.getCName()}PO po,@Param("token") Token token);

    /**
    * 逻辑删除
    *
    * @param po 删除对象
    * @return 返回数量
    */
    long delete(@Param("po") ${table.getCName()}PO po,@Param("token") Token token);

    /**
    * 更新
    *
    * @param po 更新对象
    * @return 返回数量
    */
    long update(@Param("po") ${table.getCName()}PO po,@Param("token") Token token);

    /**
     * 查询
     *
     * @param po 查询对象
     * @return 返回对象
     */
    List<${table.getCName()}PO> find(@Param("po") ${table.getCName()}PO po,@Param("beginIndex") int beginIndex,@Param("endIndex") int endIndex,@Param("pageSize") int pageSize,@Param("token") Token token);

    /**
     * 查询数量
     *
     * @param po 查询参数
     * @return 返回数量
     */
    long findCount(@Param("po") ${table.getCName()}PO po,@Param("token") Token token);

    /**
    * 查询所有
    *
    * @return 返回对象
    */
    List<${table.getCName()}PO> findAll(@Param("po") ${table.getCName()}PO po,@Param("token") Token token);
}
