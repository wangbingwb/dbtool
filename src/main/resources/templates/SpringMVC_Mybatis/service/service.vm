package ${basePackage}.${moduleName}.svc;

import ${basePackage}.${moduleName}.po.${table.getCName()}PO;
import ${basePackage}.${moduleName}.cond.QueryCondition;
import ${basePackage}.${moduleName}.rst.QueryResult;
import org.apache.ibatis.annotations.Param;

/**
* ${table.tableComment}
* @author
* @Time ${yyyy-MM-dd}
*/
public interface ${table.getCName()}Service {

    /**
    * 插入
    *
    * @param po 实体对象
    * @return 返回数量
    */
    long insert(${table.getCName()}PO po);

    /**
    * 逻辑删除
    *
    * @param po 删除对象
    * @return 返回数量
    */
    long delete(${table.getCName()}PO po,long expect);

    /**
    * 物理删除
    *
    * @param po 删除对象
    * @return 返回数量
    */
    long deleteReal(${table.getCName()}PO po);

    /**
    * 更新
    *
    * @param po 实体对象
    * @return 返回数量
    */
    long update(${table.getCName()}PO po);

    /**
    * 查询
    *
    * @param condition 查询参数
    * @return 返回对象
    */
    QueryResult<${table.getCName()}PO> query(QueryCondition condition);
}
