package ${basePackage}.${moduleName}.response;

import java.util.List;
import ${basePackage}.framework.FindResponse;
import ${basePackage}.${moduleName}.po.${table.getCName()}PO;

/**
 * ${table.getCName()}FindResponse - ${table.tableComment}
 * @author
 * @Time  ${yyyy-MM-dd}
 */
public class ${table.getCName()}FindResponse extends FindResponse<${table.getCName()}PO>{
}